/**
 * CryptoButton, an easy to use widget to create donation buttons for crypto
 * currencies
 */

Array.prototype.forEach.call(document.getElementsByClassName("crypto-button"),
		function(node) {
			
		});
